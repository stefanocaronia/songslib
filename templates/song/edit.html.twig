{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {% trans %}Edit.Song{% endtrans %}{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-header bg-default">
            <h1>{% trans %}Edit.Song{% endtrans %}</h1>
        </div>
        <div class="card-body">
            {% include 'song/form.html.twig' %}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('song-form') }}
{% endblock %}